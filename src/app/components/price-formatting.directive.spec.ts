import { PriceFormattingDirective } from './price-formatting.directive';

describe('PriceFormattingDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceFormattingDirective();
    expect(directive).toBeTruthy();
  });
});
